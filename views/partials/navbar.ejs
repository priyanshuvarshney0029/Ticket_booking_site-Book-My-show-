<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <a class="navbar-brand text-warning" href="#"><strong>Showtime</strong></a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-light" aria-current="page" href="/movies"><strong>Home</strong></a>
          </li>
          <% if(currentUser && currentUser.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link text-light" href="/movie/new"><strong>New Release</strong></a>
            </li>
          <% } %>
        </ul>
        <div class="navbar-nav ms-auto">
          <form class="d-flex" role="search" method="get" action="/search">
            <input class="form-control me-2 search-inp" type="search" placeholder="Search movies" aria-label="Search" name="search">
            <button id="main-search" class="btn btn-danger search-btn" type="submit">
              <i class="fa-solid fa-magnifying-glass"></i> Search
            </button>
          </form>
        </div>
        <div class="d-flex ms-3" role="search">
          <% if(!currentUser) { %>
            <a href="/login" class="nav-link text-warning mx-2"><strong>Login</strong></a>
          <% } else { %>
            <a href="#" class="nav-link"><strong>Hello, <%= currentUser.username %></strong></a>
            <a href="#" class="nav-link text-warning mx-2">
              <i class="fa-solid fa-cart-shopping"></i>
              <sup><span class="badge bg-danger">2</span></sup>
            </a>
            <a href="/logout" class="nav-link text-warning mx-2"><strong>Logout</strong></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
  